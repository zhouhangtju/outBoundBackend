spring:
  profiles:
    active: test
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  task:
    scheduling:
      # 是否启用定时任务执行器，默认true
      enabled: true
      pool:
        size: 2
        thread-name-prefix: schedule-

# FTP配置
ftp:
  host: 10.76.148.39
  port: 21
  username: JIAKE
  password: J8is_F7n61#12
  remote-dir: /home/JIAKE/OCP
  passive-mode: true
  connect-timeout: 10000
  data-timeout: 10000

# 定时任务配置
schedule:
  enabled: true
  cron: 0 0 19 * * ?